import * as actionTypes from '../comm/actionTypes';

export default function favors(state = {
    updateTime: null,
    favors: []
}, action) {
  switch(action.type) {
		
    	

    	default:
        return state;
  }
}